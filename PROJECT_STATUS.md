# PROJECT STATUS

Updated: 2026-08-31

## Purpose

PC에서 실제로 반복 사용하고 싶은 성경 읽기 페이지를 먼저 만든다. 개인사용 품질이 우선이며 공개 서비스·광고·사업화는 후속 Gate다.

## Current stage

**MVP v0.3 기능 확장 / 검색·구약신약 분류·빠른 구절 이동 구현 / 실제 PC 브라우저 사용검수 대기**

## Implemented

- 정적 HTML/CSS/JavaScript 구조
- 개역한글 1961 권별 JSON을 필요할 때 불러오는 구조
- 66권 선택
- 장 선택
- 이전 장 / 다음 장
- 키보드 좌우 화살표 장 이동
- 본문 중심 PC 레이아웃
- 글자 크기 조절
- 본문 폭 3단계 조절
- 라이트/다크 모드
- 절 클릭 복사
- 마지막 읽던 책/장 저장
- 읽기 설정 로컬 저장
- 창세기 1장 / 요한계시록 마지막 장에서 이전·다음 이동 버튼 경계 비활성화
- 키보드 장 이동 시 불필요한 브라우저 기본 스크롤 방지
- 장 전환 시 표준 scroll behavior 사용
- 전체 / 구약 / 신약 빠른 분류 버튼
- 구약 39권 / 신약 27권 기준으로 책 선택 목록 필터
- 성경 전체 본문 검색
- 검색 진행상태 표시
- 검색결과 최대 100개 표시
- 검색결과에서 책·장·절 바로 이동 및 해당 절 강조
- `/` 키로 검색창 빠른 포커스
- 검색창에 성경 주소 직접 입력 후 즉시 이동
- `요 3:16`, `요한복음 3장`, `롬 8:28` 등 한국어 책 이름·통상 약어 인식
- 존재하지 않는 장·절 입력 시 오류 안내

## Competitor UX benchmark — 2026-08-31

현재 공개된 Bible.com(YouVersion), Bible Gateway, Blue Letter Bible, 대한성서공회 웹 성경의 읽기·탐색 방식을 비교했다.

우리 MVP에 우선 수용할 가치가 큰 패턴:

1. 검색창을 키워드 검색과 성경 주소 바로가기에 함께 사용
2. 책 선택에서 빠르게 책을 찾고 장으로 이동
3. 두 역본을 나란히 비교
4. 절 선택 후 복사·하이라이트처럼 본문 바로 옆에서 실행되는 도구
5. 마지막 읽던 위치와 읽기 설정 기억

현재 MVP에 바로 넣지 않을 것:

- 읽기 계획·묵상 콘텐츠·영상·자료실 등 본문 읽기 핵심에서 먼 확장 기능
- 로그인 강제
- 서버 동기화가 필요한 개인 데이터 기능
- 권리 검증 전 현대 번역본 추가

## Search design

현재 검색은 별도 서버나 검색엔진 없이 클라이언트에서 개역한글 66권 JSON을 검색한다.

- 이미 읽은 책은 메모리 cache를 재사용한다.
- 처음 검색할 때는 6권 단위 batch로 본문을 읽어 브라우저·네트워크 부하를 줄인다.
- 최대 100개 결과까지만 화면에 표시한다.
- 검색결과 선택 시 해당 장으로 이동하고 목표 절을 잠시 강조한다.
- 검색창에 성경 주소 형식이 들어오면 전체 본문 검색보다 먼저 주소로 해석해 즉시 이동한다.
- 현재 개인사용 MVP에서는 서버·백엔드를 추가하지 않는다.

## Data

초기 MVP 데이터 소스:
`bluesaurel/Korean-Bible-1961-KRV`

- 본문: 성경전서 개역한글판 1961
- 표시: 대한성서공회
- 공개 서비스 전 공식 본문과 무결성 최종 검수 필요
- 현재 앱은 외부 raw GitHub JSON을 런타임에 읽는다. MVP 확인 후 자체 정적 데이터로 고정할지 결정한다.
- 저장소의 `John.json` 구조가 현재 adapter가 기대하는 `chapters → chapter → verses → verse/text` 형식과 일치함을 확인했다.

## QA completed

- 코드 기준 66권 selector와 책 간 이전/다음 장 이동 로직 검토
- 이전 책 마지막 장 이동 시 chapter clamp 동작 확인
- 성경 전체 첫 장/마지막 장에서 무반응 버튼이 남던 UX 문제 수정
- 비표준에 가까운 `scrollTo(... behavior: "instant")` 사용을 표준 `auto`로 변경
- 좌우 화살표 이동 시 기본 브라우저 스크롤 간섭 방지
- 구약/신약 경계는 말라기(39번째) / 마태복음(40번째) 기준으로 분류
- 검색 결과 이동 시 현재 책 필터와 충돌하면 전체 목록으로 자동 복귀하도록 처리
- 빠른 구절 이동은 실제 책 데이터 로딩 후 장·절 존재 여부를 확인하도록 처리

## Not implemented yet

- 두 역본 나란히 비교
- 추가 역본 adapter 실제 연결
- 책 이름 자체를 입력하며 필터링하는 책 선택기
- 북마크
- 하이라이트 저장
- 메모
- 특정 절 URL/딥링크
- 오프라인 사용
- 공개 배포
- 모바일 세부 QA

## Next

1. 실제 PC 브라우저에서 v0.3 실행 확인
2. 전체 / 구약 / 신약 버튼과 책 선택 UX 실제 검수
3. 키워드 검색과 `요 3:16` 형태 빠른 이동 실제 동작·속도 검수
4. PC 읽기 화면의 글자·행간·폭·상단 이동 UX 사용자 기준 조정
5. 첫 실제사용 피드백 오류·불편 수정
6. 권리 안전한 두 번째 역본을 연결한 뒤 두 역본 나란히 비교 구조 구현
7. 필요하면 책 선택기를 드롭다운보다 빠른 검색형 선택기로 개선
8. 개인사용 반복 테스트 후 공개 서비스 여부 판단
