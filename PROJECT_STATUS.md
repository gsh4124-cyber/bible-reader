# PROJECT STATUS — bible-reader

- 마지막 갱신: 2026-09-02
- 저장소 역할: 성경 읽기 웹도구의 코드·기술상태·현재 상태 인계 원본
- 상위 상태 원본: 황제 Vault `자동 사업운영/바이브코딩/_INDEX.md`
- ChatGPT 실행창: `성경 홈페이지 본부`
- 표준 로컬 경로: `C:/Users/gsh41/Desktop/황제/자동 사업운영/바이브코딩/bible-reader`
- 운영 주소: https://gsh4124-cyber.github.io/bible-reader/

## 현재 단계 — 황제 확정 2026-09-02

**현재 버전을 최종 기준본으로 확정. 기능 확장은 일단 종료하고 유지보수·실사용 QA 단계로 전환한다.**

새 기능을 상상해서 계속 붙이지 않는다. 실제 사용 중 발견되는 오류·불편, 검색노출 문제, 외부 데이터 로딩 문제, 브라우저 회귀처럼 실제 근거가 생긴 경우만 수정한다.

## 핵심 목적

> **PC에서 실제로 가장 편하게 읽을 수 있는 성경 페이지를 만든다.**

기능 수보다 장시간 읽기 편안함, 빠른 위치 이동, 역본 비교, 기록 회수, 실제 사용 피드백을 우선한다.

## 최종 실행선

- 정적 HTML/CSS/JavaScript
- 66권·장·절 이동, 구약/신약/전체 선택
- 상단 통합 검색. 입력창 placeholder는 `검색`으로만 표시
- 성경책 위치 입력과 본문 단어 검색
- 이전/다음 장 이동
- 글자 크기, 본문 폭, 라이트/다크 모드
- 한 면 보기 / 양면 보기
- 다국어 역본 선택 및 2역본 비교
- 비교 진입 시 현재 메인 역본을 왼쪽 비교 역본으로 유지
- 비교 역본 선택줄은 sticky가 아니며 스크롤과 함께 사라짐
- 절 복사
- 절 하이라이트
- 절 저장
- 장 저장
- `나의 기록` 패널
  - 탭 순서: `하이라이트 / 저장한 성구 / 저장한 장`
  - 하이라이트·저장한 성구·저장한 장 모두 메모 추가/수정 가능
  - 각 항목에서 해당 하이라이트/저장 상태 삭제 가능
  - 삭제 시 그 항목에 붙은 기록 메모도 같이 정리
  - 패널 바깥 클릭 시 패널 닫힘
  - 메모 편집창 바깥 클릭 시 메모창 닫힘
- 기록 백업/복원(JSON)
- 마지막 읽기 위치·역본·읽기 설정·기록은 브라우저 localStorage에 저장
- 로그인/계정/서버 DB/동기화 없음
- 광고 슬롯 구조는 존재하지만 실제 광고는 비활성

## 제거·정리된 실행선

황제의 실제 사용 결과 집중모드형 기본 화면이 더 좋다고 판단해 다음을 제거했다.

- 오늘의 성구
- 오늘의 묵상장
- 별도 집중모드 버튼/기능

검색 placeholder를 다시 긴 안내문으로 덮어쓰던 다국어 처리도 수정해 현재 `검색`만 유지한다.

최종 정리에서 다음 오래된/중복 실행 파일을 제거했다.

- `daily.js`
- `focus-mode.js`
- 구형 `locale.js`
- 중복 `notebook-tabs-fix.css`

현재는 `locale-safe.js`를 사용하며 기록 확장은 `records-enhancements.js/css`가 담당한다.

관주·난하주 관련 `annotations-data.js`, `annotations.js`, `REFERENCE_DATA_IMPORT.md`, 검증 도구는 **현재 runtime에서 제외된 미래 자산**으로만 보존한다.

## 데이터·권리 기준

기본 한국어 본문:
- 개역한글 1961
- `bluesaurel/Korean-Bible-1961-KRV`

현재 권리 안전 실행선:
- KJV
- WEB
- ASV
- Louis Segond 1910
- Lutherbibel 1912
- 和合本(CUV)
- Russian Synodal
- Vulgata

외국어 공개 역본 데이터는 `midvash/bible-data` 권별 JSON을 사용한다.

개역개정·새번역·공동번역 등 현대 한국어 역본은 권리자 허가·라이선스 없이 포함하지 않는다.

난하주·관주는 신뢰 가능한 전체 원자료 확보 전까지 실제 runtime에 넣지 않는다.

## 개인 기록 저장 원칙 — 황제 확정

현재는 **localStorage only**다.

저장 대상:
- 마지막 위치
- 선택 역본
- 보기 설정
- 하이라이트
- 저장 성구
- 저장 장
- 기록별 메모

서버 계정·로그인·클라우드 동기화는 비용과 운영복잡성을 이유로 현재 제외한다. 서버 개인정보 저장을 다시 도입하려면 별도 Gate다.

브라우저·프로필·기기별 데이터가 분리되고 사이트 데이터 삭제 시 기록이 사라질 수 있으므로 JSON 백업/복원을 제공한다.

## 공개 배포·검색노출

- GitHub 저장소 공개 상태
- GitHub Pages 자동배포: `main` push → `Final QA and Deploy Pages`
- 현재 최종 기능 커밋 `a92d7f16f342e82bdd38fe4ed6ff727d24cf8318`
- 해당 배포 workflow run `33516162493` 성공 확인
- Google Search Console URL-prefix 소유권 확인 완료
- HTML verification tag는 `<head>`에 계속 유지
- canonical / robots / sitemap 실행선 유지

## 최종 사용 판단 — 황제 확정 2026-09-02

황제가 실제 PC에서 반복 확인 후 현재 상태를 **최종 기준본**으로 확정했다.

특히 확정된 UX 방향:
- 집중모드처럼 단순한 기본 화면
- 상단 불필요 카드 제거
- 검색창은 짧게 `검색`
- 기록은 별도 메모 탭보다 실제 저장 대상에 메모를 붙이는 방식
- 기록 패널 탭은 `하이라이트 / 저장한 성구 / 저장한 장`
- 하이라이트와 저장은 한 번 클릭 흐름을 유지하고 저장 시 메모 여부를 강제 질문하지 않음
- 필요할 때 기록 목록에서 메모 추가

## 재발 방지 규칙

1. **UI를 제거하면 HTML만 숨기지 말고 연결된 JS/CSS/QA 필수파일 목록까지 같이 정리한다.**
2. 구형 파일을 제거한 뒤 GitHub Actions가 삭제된 파일을 여전히 필수 runtime으로 검사하지 않는지 확인한다.
3. 정적/자동 QA PASS를 실제 사용 PASS로 간주하지 않는다. 최종 UI는 황제의 실제 PC 사용 결과를 우선한다.
4. 다국어 코드가 placeholder·제목·컨트롤을 다시 덮어쓰는지 확인한다.
5. 기록 패널·메모창처럼 겹치는 layer는 z-index뿐 아니라 실제 클릭 대상과 바깥 클릭 닫기를 사용자 동작으로 확인한다.
6. 같은 기능을 새 보조 파일로 계속 덮어씌우지 말고, 마무리 단계에서 중복·죽은 코드와 stylesheet를 정리한다.
7. 원격 `main` 반영과 로컬 저장소 동기화는 별개다. 로컬 실행경로를 사용할 수 있는 환경에서는 작업 종료 전에 최신 origin 동기화를 확인하고, 실제로 못 한 경우 했다고 보고하지 않는다.

## 유지보수 기준

현재부터는 새 기능 개발보다 아래를 우선한다.

- 실제 사용 중 발견되는 불편·오류
- 핵심 기능 회귀
- GitHub Pages 배포 이상
- 외부 성경 JSON 로딩 문제
- Search Console 색인·검색 오류
- 브라우저 호환성 변화
- 번역본 권리·출처 변화
- localStorage/백업 복원 문제

주간·월간 전체 Project 정기점검 대상에 포함한다. 별도 성경 전용 일일 Task는 현재 만들지 않는다.

## Gate

다음은 황제 확인 전 진행하지 않는다.

- 실제 광고/AdSense 도입
- 새 비용·유료도구
- 유료 번역본 라이선스
- 외부 권리자 연락
- 로그인/서버 DB/개인정보 저장
- 큰 제품 방향 변경

## 다음 행동

**기능 개발 없음.**

실사용·검색노출을 관찰하고 실제 문제가 생길 때만 유지보수한다. 현대 한국어 번역본은 권리 해결 시 별도 검토한다.
