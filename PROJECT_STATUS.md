# PROJECT STATUS — bible-reader

- 마지막 갱신: 2026-09-01
- 저장소 역할: 성경 읽기 웹도구의 코드·기술상태·현재 상태 인계 원본
- 상위 상태 원본: 황제 Vault `자동 사업운영/바이브코딩/_INDEX.md`
- ChatGPT 실행창: `성경 홈페이지 본부`
- 표준 로컬 경로: `C:/Users/gsh41/Desktop/황제/자동 사업운영/바이브코딩/bible-reader`

## 현재 단계

**MVP v0.7 UI 구조 정리·회귀오류 방지 완료 / 실제 PC 재검수 대기**

현재는 개인사용 품질이 우선이며 공개 서비스·실제 광고 노출·사업화는 후속 Gate다.

## 핵심 목적

> PC에서 실제로 가장 편하게 읽을 수 있는 성경 페이지를 만든다.

## 현재 구현

- 정적 HTML/CSS/JavaScript
- 개역한글 권별 JSON lazy loading
- 전체 / 구약 / 신약 드롭다운 분류
- 66권·장·절 선택
- 책 이름·본문 단어·`요 3:16` 통합 검색
- 검색결과 절 이동·강조
- 하단 이전 장 / 다음 장 이동
- PC 중심 읽기 레이아웃
- 글자 크기·본문 폭·라이트/다크 모드
- 절 클릭 복사 및 장·절 표기 포함
- 마지막 위치·읽기 설정 저장
- 개역한글 + KJV 병렬 비교
- 비교 역본 좌/우 선택 및 교환
- 비교 역본 선택 상태 로컬 저장
- 날짜별 `오늘의 성구`
- 날짜별 `오늘의 묵상장`
- 광고용 상·하단 슬롯 구조 준비(현재 숨김)
- 난하주·소제목 annotation 스키마 및 렌더러 준비

## 사고분리·검증 순서 — 2026-09-01

한 실행창에서 요구정리·구현·검수를 연속 수행해도 다음 단계를 같은 자기확인 사고로 합치지 않는다.

> 실제 사용 요구·불편 → 요구사항 잠금 → 구현 → 정적/자동 검사 → 독립 회귀 QA → 황제 실제 PC 사용검증 → 다음 수정

### 요구사항

- 황제의 실제 PC 사용 피드백과 확정 요청을 먼저 잠근다.
- 개발 편의를 위해 요청의 의미를 조용히 바꾸지 않는다.
- 기능 아이디어와 황제 확정 요구사항을 구분한다.

### 구현

- 잠긴 요구사항을 코드로 구현한다.
- 구현 중 새 UX 결정을 임의 확정하지 않는다.
- UI를 제거·교체하면 관련 HTML뿐 아니라 JS 참조·CSS·상태까지 함께 정리한다.

### 정적/자동 검사

- 문법·DOM 참조·기본 로딩·명백한 회귀를 찾는다.
- 자동 검사가 통과했다는 이유로 실제 읽기 경험까지 통과했다고 보지 않는다.

### 독립 회귀 QA

구현자의 의도보다 실제 동작을 본다.

- 제거한 컨트롤의 잔여 참조가 없는가
- 기존 검색·선택·비교 기능이 깨지지 않았는가
- 경계조건과 상태복원이 실제로 작동하는가
- 새 UI가 다른 기능의 클릭·포커스·레이아웃을 방해하지 않는가

`코드상 맞다`는 설명은 PASS 근거가 아니다.

### 실제 PC 사용검증

> 구현 완료 ≠ 자동검사 통과 ≠ 회귀 QA 통과 ≠ 실제 사용성 검증 완료

최종 읽기 편의성은 황제가 실제 PC에서 사용한 결과로 확인한다. 실제 사용에서 문제가 나오면 QA를 통과했더라도 원인 단계로 되돌린다.

## 2026-09-01 구조 정리

실제 PC 피드백을 반영하는 과정에서 UI를 제거·교체한 뒤 예전 DOM 참조와 호환용 코드가 남아 회귀오류가 반복되는 문제가 확인됐다.

v0.7에서 다음을 정리했다.

- 삭제된 상단/양옆 화살표 DOM 참조를 안전 처리
- 화면에서 사라진 `전체/구약/신약` 옛 버튼 3개를 HTML에서 완전 제거
- 범위 드롭다운이 숨은 버튼을 대신 클릭하던 구조 제거
- `testamentSelect`가 `setTestament()`를 직접 호출하도록 변경
- 책 이름 검색이 숨은 구약/신약 버튼에 의존하던 구조 제거
- 양옆 화살표의 잔여 CSS 제거
- 현재 실제 UI에 존재하는 컨트롤을 기준으로 연결 구조 재정렬

앞으로 UI 요소를 삭제·교체할 때는 HTML만 바꾸지 않고 관련 JS 참조·CSS·상태문서까지 같이 제거한 뒤 완료 처리한다.

## 본문·관주·소제목 데이터 상태

현재 기본 데이터 `bluesaurel/Korean-Bible-1961-KRV`에는 본문 외 별도 소제목·난하주 필드가 없다.

따라서 난하주·소제목을 AI가 임의 생성하거나 다른 번역본의 편집정보를 섞지 않는다.

`annotations-data.js` 구조:
- 장별 소제목: `BookFile:chapter → [{ verse, text }]`
- 절별 난하주: `BookFile:chapter:verse → [{ marker, text }]`

검증된 원자료를 확보하면 현재 렌더러에 연결한다.

권리 후보:
- 대한성서공회 안내 기준 `관주성경전서 개역한글판`(1962)의 저작재산권 보호기간은 종료됨.
- 실제 디지털 난하주·소제목 원자료의 출처·정합성 확인과 성명표시권·동일성유지권 준수가 필요하다.

## 번역본·권리

- 개역한글: 기본 한국어 역본. 저작재산권 보호기간 종료, 성명표시권·동일성유지권 준수 필요
- KJV: Public Domain 기반 비교 역본
- Korean Bible 1910: 향후 한국어 비교 후보
- 주요 현대 한국어 역본은 권리자 허가 전 포함하지 않음
- 성경 본문 접근과 핵심 읽기 기능은 유료 잠금하지 않음

## 현재 검수 포인트

1. Pull 후 본문이 정상 로드되는지
2. 전체/구약/신약 드롭다운과 책 목록이 정상 연동되는지
3. 책 이름 검색·본문 검색·구절 바로가기가 서로 충돌하지 않는지
4. 장·절 선택과 하이라이트가 정상인지
5. 비교 ON/OFF와 역본 교환이 정상인지
6. 오늘의 성구·오늘의 묵상장 클릭 이동이 정상인지
7. 하단 이전 장 / 다음 장이 첫 장·마지막 장 경계에서 정상인지

## 다음 행동

1. GitHub Desktop Pull
2. 실제 PC에서 v0.7 회귀검수
3. 오류가 없으면 UI 안정화 단계 종료
4. 그 후 1962 관주개역한글의 신뢰 가능한 원자료 확보 및 난하주·소제목 실제 연결
