---
type: canonical
status: confirmed
source_role: emperor
updated: 2026-09-03
---

# 바이블리더 다국어 운영상태

## 황제 확정 방향

바이블리더는 한국어 본체와 별도 해외 미니앱을 운영하지 않는다.

> **동일한 실제 성경 리더 본체를 모든 언어에서 사용하고, UI 언어·기본 역본·검색 진입 URL만 현지화한다.**

IP 기반 강제 리다이렉트는 사용하지 않는다.

## 가장 중요한 구분

**UI 언어와 선택한 성경 역본의 언어는 서로 독립한다.**

- **UI 언어** — 검색, 비교, 이전/다음, 기록, 메모, 설정, 오류·로딩 등 인터페이스 문구를 결정한다.
- **선택 역본** — 성경 본문, 성경책 이름, 장 제목, 성구 참조·복사에 쓰이는 언어를 결정한다.

예:
- 한국어 UI + KJV 선택 → 인터페이스는 한국어, 성경 본문·책 이름·참조는 영어
- 영어 UI + CUV 선택 → 인터페이스는 영어, 성경 본문·책 이름·참조는 중국어

역본을 바꿨다는 이유로 UI 언어를 자동 변경하지 않고, UI 언어를 바꿨다는 이유로 사용자가 직접 선택한 역본을 임의로 덮어쓰지 않는다.

이 구분은 과거의 “현재 UI 언어의 책 이름을 사용한다”는 단순화보다 우선하는 최신 기준이다.

## 현재 지원 UI 언어·URL

- 한국어 `/bible-reader/`
- English `/bible-reader/en/`
- Français `/bible-reader/fr/`
- Deutsch `/bible-reader/de/`
- 中文 `/bible-reader/zh/`
- Русский `/bible-reader/ru/`
- Latina `/bible-reader/la/`
- Português `/bible-reader/pt/`
- العربية `/bible-reader/ar/`

각 언어 URL은 소개용 페이지가 아니라 동일한 실제 리더 본체를 부팅한다.

현재 기본 역본은 언어별 진입 경험을 위해 각각 개역한글 1961, KJV, Louis Segond 1910, Lutherbibel 1912, 和合本 CUV, Russian Synodal, Vulgata, Almeida 1819, Smith–Van Dyck를 사용한다.

## 현재 구현 원칙

- 9개 UI 언어를 지원한다.
- 실제 사용 가능한 성경 역본만 선택기에 노출한다.
- 성경 66권 이름은 **선택한 역본의 언어**에 맞춰 표시한다.
- 검색·참조·복사·장 제목도 선택 역본의 성경 언어를 따른다.
- 기록의 핵심 식별자는 번역문 문자열이 아니라 성구 위치다.
- 하이라이트·저장 성구·저장 장·메모는 브라우저 localStorage에 유지한다.
- 기록 목록의 성경 본문은 현재 선택 역본 기준으로 다시 표시할 수 있다.
- PC에서는 가능한 한 한 줄과 읽기 집중도를 유지하되 긴 언어에서는 잘림보다 자연스러운 줄바꿈을 우선한다.
- **Arabic도 전체 UI shell은 기존 레이아웃 안정성을 위해 LTR로 유지하고, 실제 아랍어 성경 본문·책명·장 제목 등 Scripture 영역의 언어·방향성을 별도 처리한다.** 전체 `<html dir="rtl">`을 강제하지 않는다.
- 언어별 URL에는 `html lang`, title, description, canonical, reciprocal hreflang, x-default를 실제 구조에 맞게 유지한다.

## 실제 Production QA — 2026-09-03

로컬 정적 QA만으로 끝내지 않고 GitHub Pages 배포 이후 **실제 공개 URL을 Chromium/Playwright로 검사하는 `Production Browser QA`를 배포 workflow에 추가**했다.

첫 검증에서 테스트가 Arabic 전체 shell을 RTL로 기대해 실패했으나, 기존 제품 기준은 전체 shell LTR + Scripture 별도 방향성이었다. 제품 버그가 아니라 검증기 기준 불일치로 확인해 테스트를 기존 확정 구조에 맞게 수정했다.

초기 검증 기준:
- workflow: `Final QA and Deploy Pages`
- run: `33749042403`
- head: `1e4635b40cecad51a8b2c4add6045cb4a6053e74`
- 결과: **SUCCESS**

Production Browser QA에서 실제로 확인한 것:
- 한국어 UI에서 KJV를 선택해도 UI 언어가 한국어로 유지됨
- KJV 선택 시 장 제목은 영어 성경 언어를 따름
- 영어 UI에서 CUV를 선택해도 UI 언어가 영어로 유지됨
- CUV 선택 시 장 제목은 중국어 성경 언어를 따름
- Arabic 진입에서 UI shell은 LTR 안정구조를 유지함
- Arabic 기본 SVD가 선택되고 장 제목은 아랍어 성경 언어를 따름
- 해당 흐름에서 pageerror 없음

따라서 **UI 언어 ↔ 선택 역본 언어 독립성의 핵심 공개환경 회귀 Gate는 Production 자동검증으로 닫는다.** 다만 iPhone Safari, 실제 native select 체감, 저장데이터 보존 등 물리기기·교차브라우저 전부를 완료했다고 확대하지 않는다.

## 운영 호스트 일관성 회복 — 2026-09-03

현재 황제 Vault와 검색엔진 등록 실행선의 확정 운영 주소는 `https://gsh4124-cyber.github.io/bible-reader/`다.

작업 중 루트 `canonical/hreflang`, `sitemap.xml`, `robots.txt`만 `https://bible-reader-1iz.pages.dev/`로 부분 변경됐지만, 언어별 entry·`/bible-reader/` runtime 라우팅·GitHub Pages 배포·Production QA·익명 page-view 허용 origin·기존 검색엔진 등록은 계속 GitHub Pages를 기준으로 하고 있었다. 즉 실제 호스트 전환이 아니라 **SEO 신호 일부만 다른 호스트를 가리키는 불일치**였다.

이 불일치는 실제 Actions에서 Static Guardrails와 Behavior QA 실패로 검출됐다. Cloudflare Pages 주소를 현재 주 운영 호스트로 전환한다는 황제의 별도 확정도 없었으므로, 기존 확정 운영선과 검색자산을 우선해 부분 변경된 세 SEO 신호를 GitHub Pages 기준으로 복구했다.

복구 검증:
- workflow run: `33756412342`
- Static Guardrails: SUCCESS
- Behavior QA: SUCCESS
- Combined Quality Gate: SUCCESS
- GitHub Pages deploy: SUCCESS
- Production Browser QA: SUCCESS
- IndexNow notify: SUCCESS

따라서 현재 운영 호스트는 계속 **GitHub Pages**이며, `bible-reader-1iz.pages.dev` 주소가 존재한다는 사실만으로 Cloudflare가 주 운영 주소 또는 AdSense 주소구조 해결책으로 승격된 것으로 보지 않는다.

재발 방지 원칙:
> **운영 호스트 변경은 canonical 한 줄 교체가 아니라 인프라 전환이다.**

향후 호스트를 바꾸려면 운영 주소 확정 → 모든 언어 canonical/hreflang → sitemap/robots → runtime base path·언어 라우팅 → analytics/API origin → 실제 배포 pipeline → 새 호스트 Production QA → 검색엔진 등록·Vault 상태 → 기존 호스트 유지/redirect/폐기 처리까지 하나의 전환 Gate로 검증한다. 이 범위를 닫기 전에는 SEO 파일 일부만 새 호스트로 선행 변경하지 않는다.

## 익명 사용 집계

이미 실제 운영 중이던 집계 실행선을 재확인했다.

구조:
- 공개 사이트 `analytics.js`
- Supabase Edge Function `bible-page-view`
- DB 집계 `public.bible_page_views_daily`
- 일별 **페이지뷰 총합만** 증가
- 사용자 계정·개인식별정보·고유 방문자 식별을 저장하는 지표로 사용하지 않음

2026-09-03 20시대 실제 DB 조회 기준:
- 2026-09-02: 9 page views
- 2026-09-03: 22 page views
- 최근 14일 합계: 31 page views

이 수치는 테스트·운영자 접속이 포함될 수 있으며 **고유 사용자 31명이나 시장수요 31건이라는 뜻이 아니다.** Vault snapshot `성경읽기_analytics_latest.json`으로 운영관제에 연결한다.

## 검색 유통

- 현재 주 운영·검색 주소는 `https://gsh4124-cyber.github.io/bible-reader/`다.
- `robots.txt`와 `sitemap.xml`을 이 운영 주소와 일치하게 유지한다.
- Google Search Console 소유권·기본 색인 경로를 사용한다.
- IndexNow 자동 제출 실행선을 유지한다.
- 중국어·러시아어는 각각 Baidu/Yandex 검색생태계 가능성을 별도로 검토한다.
- 제출 성공, 크롤링, 색인, 검색노출, 실제 유입은 서로 다른 상태로 기록한다.

현재 외부 검색등록·색인 상태의 원본은 [[검색엔진_등록_상태]]다.

## 권리 기준

성경 본문은 공개도메인 또는 자유 재배포 가능성이 확인된 실행선만 사용한다.

개역개정·새번역·공동번역 등 현대 한국어 역본을 권리자 허가·라이선스 없이 포함하지 않는다.

## 코드·상태 원본

실제 기술 상태 원본은 `gsh4124-cyber/bible-reader`의 최신 `main`과 `PROJECT_STATUS.md`다.

이 Vault 문서는 장기적으로 유지할 언어·역본 분리 원리, 공개 검증 상태, 사업 운영 Gate를 관리한다.

## 아직 현실검증이 필요한 것

Production Browser QA로 핵심 언어/역본 독립성은 실제 공개 Chromium에서 확인됐다. 남은 현실검증은:

- 실제 iPhone Safari / Android / 주요 데스크톱 브라우저의 native select 체감
- 장·절 이동의 실제 모바일 조작성
- 더 많은 UI 언어 × 다른 역본 조합
- 기존 저장 성구·하이라이트·메모 보존의 장기 회귀
- Search Console·IndexNow 이후 실제 언어별 색인·검색 노출·유입

## AdSense

현재 공개 주소가 `gsh4124-cyber.github.io/bible-reader/`의 **경로형 GitHub Pages URL**이므로, 별도 AdSense 사이트 등록 구조는 현재 `ADSENSE_ADDRESSABILITY_BLOCKED`로 관리한다.

실제 광고 도입은 [[AdSense_승인_운영기준|AdSense 승인 운영기준]]을 따른다. 자체 도메인 등 주소구조 변경이 실제로 최선이 되는 시점에 비용·브랜드·검색자산 효과까지 함께 비교한다.

## 현재 다음 행동

> Production 자동검증 유지 → 실제 물리기기/교차브라우저에서 남은 고위험 부분만 검증 → 검색노출·익명 페이지뷰 관찰 → 주소구조/AdSense Gate 판단

순서로 운영한다.
